import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="API Reference/useMounted" />

# useMounted

Track if component is mounted.

```js
const isMounte = useMounted()
```

## Returns

* `isMounte: boolean` - mounted or not.